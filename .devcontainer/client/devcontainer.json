{
    "name": "Project client",
    "service": "client",
    "dockerComposeFile": "../../docker-compose.dev.yaml",
    "workspaceFolder": "/app",
    "customizations": {
        "vscode": {
            "extensions": [
            ],
            "settings": {

            }
        }
    }
}